# 🟦 1. **Сравнение имени отправителя и e-mail**

В письме всегда есть два элемента, которые видит пользователь:

```
От кого: "PayPal Support" <secure@paypal.com>
```

* **Отображаемое имя (Display Name)** → *"PayPal Support"*
* **Адрес (Email Address)** → *[secure@paypal.com](mailto:secure@paypal.com)*

### 🔍 Что проверяется:

### **(1) Подозрительные или слишком общие имена**

Если имя отправителя:

* слишком общее («Customer Service», «Support Team»)
* написано странно («PayPaI Supp0rt», подмена символов)
* содержит много спецсимволов
* использует ложный бренд («Microsoft Billing Center FREE!»)

→ Это сигнал фишинга.

### **(2) Несоответствие имени и адреса**

Например:

```
"Apple Support" <security@randomdomain.info>
```

То есть имя → «Apple Support»,
а домен письма → *randomdomain.info*.

Если имя указывает на бренд, а домен — нет → почти 100% фишинга.

### **(3) Использование чужого бренда в имени**

Если имя содержит:

* PayPal
* Google
* Microsoft
* Apple
* Netflix

Но домен отправителя НЕ принадлежит этой компании → фишинг.

---

# 🟦 2. **Проверка отображаемого имени vs реальный отправитель**

Email-клиент показывает человеку красивое имя:

```
От: "СберБанк" <info@sberbank-support.ru>
```

Большинство людей НЕ смотрят на e-mail, а верят имени.
Но злоумышленник может написать:

```
"Netflix Billing Support" <service@nxetflix-pay.info>
```

Всё выглядит «правдоподобно», но это мошенники.

### 🔍 Что скрипт должен проверить:

### **(1) Реальный email-домен**

Выделяем домен:

* `sberbank-support.ru`
* `nxetflix-pay.info`
* `microsoft-login-secure.com`

И проверяем:

* Легитимный ли он?
* Похож ли на известный бренд?
* Есть ли подмена символов? (например, замена `l` → `I`, `o` → `0`)

### **(2) Проверка на look-alike домены**

Примеры фишинга:

| Настоящий   | Подменный          |
| ----------- | ------------------ |
| sberbank.ru | sberbank-secure.ru |
| paypal.com  | paypa1.com         |
| netflix.com | netfIix.com        |

Скрипт будет проверять:

* расстояние Левенштейна между доменами
* наличие подставок (`-secure`, `-login`, `-verify`)
* похожие TLD (.info, .xyz, .top)

### **(3) Несоответствие имени бренду**

Если отправитель называется «Google Support»,
но домен → `googIe-security.com` (подмена L → I),

скрипт это обнаружит.

---

# 📌 **Пример алгоритма проверки (на русском)**

### 1. Извлечь отображаемое имя:

```
display_name = "Netflix Billing Support"
```

### 2. Извлечь email:

```
email = "service@nxetflix-pay.info"
```

### 3. Извлечь домен:

```
domain = "nxetflix-pay.info"
```

### 4. Проверить:

* совпадает ли имя с доменом
* домен похож на настоящий?
* нет ли замены символов?
* не использованы ли подозрительные суффиксы (`-verify`, `-secure`)
* домен старый или свежезарегистрированный?
* имя бренда присутствует в Display Name, но домен ему не соответствует

---

# 📌 Итоговая логика

### **Если:**

Имя отправителя → содержит бренд
НО
домен → НЕ принадлежит этому бренду

→ Письмо подозрительное.

### **Если:**

Имя отправителя очень отличается от email-адреса
(например, имя «Сергей К.» → email `noreply@randomhost.mail`)

→ это повод проверить письмо глубже.

### **Если:**

Имя отправителя и email выглядят очень странно или слишком общо
(«Customer Service Agent», «Official Notice»)

→ тоже признак фишинга.